Base paper surface with a soft warm shadow — wraps almanac, blessing, and detail panels.

```jsx
<Card>今日黄历…</Card>
<Card tone="gold" padding="var(--sp-6)">节日提醒</Card>
```

- `tone`: `default` · `sunken` · `gold` (hairline gold frame) · `brand` (red-tint)
- `padding` accepts any CSS value.
