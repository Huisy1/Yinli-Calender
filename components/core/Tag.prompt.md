Pill filter chip with a red selected state.

```jsx
<Tag selected>节日</Tag>
<Tag onClick={pick}>节气</Tag>
```

Use for blessing categories or list filters. For a 2–3 option view switch prefer `SegMenu`.
