Segmented control for the calendar view switch and other 2–4 option toggles.

```jsx
<SegMenu items={["月","周","日"]} value={view} onChange={setView} />
```

Selected segment fills 中国红. Items may be strings or `{value, label}`.
